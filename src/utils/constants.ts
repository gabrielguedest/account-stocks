export enum Constants {
  Sequelize = 'sequelize',
  UserRepository = 'user_repository',
  CheckingAccountRepository = 'checking_account_repository',
  UserStocksRepository = 'user_stocks_repository',
  StocksRepository = 'stocks_repository',
}
